# 需求文档：用户输入节点

## 目标

定义 `user_input` 节点在 Cube 中的人工输入采集能力。

## 需求

### 需求 1：等待输入

系统应允许 `user_input` 节点将实例置为 `waiting_input` 状态。

### 需求 2：输入采集

节点应支持文本输入、结构化输入或附件补充输入。

### 需求 3：恢复执行

提交输入后应支持从当前节点恢复执行并保留输入记录。
