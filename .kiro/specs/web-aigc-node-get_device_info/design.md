# 设计文档：获取设备信息节点

## 设计概述

`get_device_info` 属于宿主设备能力节点，在 Cube 桌面/浏览器环境中价值有限，优先级较低。

## 接口映射

- `web-aigc` 节点：`get_device_info`
- Cube 承接：前端环境信息适配器

## 运行流程

1. 请求可用设备信息
2. 过滤隐私字段
3. 输出结构化设备信息
4. 供下游节点使用
