# 任务清单：接管面板与决策点

- [ ] 定义 Takeover Point 领域模型
  - [ ] 定义 `TakeoverPoint`。
  - [ ] 定义 `TakeoverType`。
  - [ ] 定义 `TakeoverStatus`。
  - [ ] 定义 `TakeoverOption`。
  - [ ] 定义 `TakeoverAction`。
  - [ ] 定义 `TakeoverTimeoutPolicy`。

- [ ] 定义接管类型与触发规则
  - [ ] 定义澄清类接管。
  - [ ] 定义路线确认类接管。
  - [ ] 定义预算确认类接管。
  - [ ] 定义权限确认类接管。
  - [ ] 定义风险接受类接管。
  - [ ] 定义交付验收类接管。
  - [ ] 定义异常接管类接管。

- [ ] 设计接管队列
  - [ ] 定义 `TakeoverQueue`。
  - [ ] 定义必须接管与建议接管的优先级。
  - [ ] 定义严重程度排序规则。
  - [ ] 定义多个低风险接管点的合并规则。
  - [ ] 定义运行时阻塞类接管的处理规则。

- [ ] 设计接管面板信息架构
  - [ ] 定义当前接管区域。
  - [ ] 定义决策上下文区域。
  - [ ] 定义可选动作区域。
  - [ ] 定义推荐默认动作区域。
  - [ ] 定义风险与证据区域。
  - [ ] 定义历史接管时间线区域。

- [ ] 设计澄清类接管体验
  - [ ] 支持单选澄清。
  - [ ] 支持多选澄清。
  - [ ] 支持自由文本澄清。
  - [ ] 支持上下文或文件引用补充。
  - [ ] 支持“使用默认并继续”。
  - [ ] 映射到 `waiting / WAITING_INPUT / resume`。

- [ ] 设计路线确认类接管体验
  - [ ] 展示快速路线。
  - [ ] 展示标准路线。
  - [ ] 展示深度路线。
  - [ ] 展示路线差异。
  - [ ] 支持路线选择后更新 Route。
  - [ ] 支持路线选择后触发重规划。

- [ ] 设计预算确认类接管体验
  - [ ] 展示预计成本。
  - [ ] 展示成本来源。
  - [ ] 支持设置最大预算。
  - [ ] 支持切换低成本路线。
  - [ ] 将预算结果映射到 runtime governance。
  - [ ] 将预算确认写入审计。

- [ ] 设计权限确认类接管体验
  - [ ] 展示请求权限。
  - [ ] 展示使用目的。
  - [ ] 展示权限范围。
  - [ ] 支持批准一次。
  - [ ] 支持批准本任务。
  - [ ] 支持拒绝。
  - [ ] 支持升级人工处理。
  - [ ] 映射到 permission / capability / approval 链路。

- [ ] 设计风险接受类接管体验
  - [ ] 展示风险类型。
  - [ ] 展示严重程度。
  - [ ] 展示触发条件。
  - [ ] 展示缓解方案。
  - [ ] 支持接受风险。
  - [ ] 支持降低风险路线。
  - [ ] 支持请求更多证据。
  - [ ] 高风险接受要求填写原因。

- [ ] 设计交付验收类接管体验
  - [ ] 展示结果摘要。
  - [ ] 展示成功标准覆盖情况。
  - [ ] 展示未解决问题。
  - [ ] 支持接受交付。
  - [ ] 支持继续修正。
  - [ ] 支持切换深度复核。
  - [ ] 支持保存草稿或终止。

- [ ] 设计异常接管体验
  - [ ] 展示失败原因。
  - [ ] 展示已尝试恢复动作。
  - [ ] 展示影响范围。
  - [ ] 支持重试。
  - [ ] 支持换路线。
  - [ ] 支持跳过非关键步骤。
  - [ ] 支持升级人工。
  - [ ] 支持终止任务。
  - [ ] 映射到 `retry / escalate / terminate / resume`。

- [ ] 设计与现有 HITL / decision 的兼容层
  - [ ] 定义 TakeoverPoint 到 MissionDecision 的映射。
  - [ ] 复用 `submitMissionDecision()` 的幂等提交语义。
  - [ ] 兼容 `MissionStore.markWaiting()`。
  - [ ] 兼容 `MissionStore.resolveWaiting()`。
  - [ ] 兼容 `MissionOrchestrator.submitDecision()`。
  - [ ] 兼容 Web-AIGC runtime 的 `WAITING_INPUT -> resume()`。
  - [ ] 兼容 `escalate()` 人工升级。

- [ ] 设计接管记录的审计与回放
  - [ ] 记录接管点生成原因。
  - [ ] 记录展示给用户的内容。
  - [ ] 记录用户可选动作。
  - [ ] 记录用户选择。
  - [ ] 记录用户补充说明。
  - [ ] 记录触发的 runtime action。
  - [ ] 关联 Route / Mission / Workflow / Runtime Event。
  - [ ] 在 replay 时间线中展示接管事件。
  - [ ] 在 audit 中展示预算、权限、风险接受记录。

- [ ] 设计降低打断策略
  - [ ] 定义建议接管不阻塞规则。
  - [ ] 定义低风险接管合并规则。
  - [ ] 定义默认动作可用条件。
  - [ ] 定义高风险必须显式确认条件。
  - [ ] 定义超时策略适用范围。

- [ ] 补充测试计划
  - [ ] 覆盖澄清类接管。
  - [ ] 覆盖路线确认类接管。
  - [ ] 覆盖预算确认类接管。
  - [ ] 覆盖权限确认类接管。
  - [ ] 覆盖风险接受类接管。
  - [ ] 覆盖交付验收类接管。
  - [ ] 覆盖异常接管。
  - [ ] 覆盖 TakeoverPoint 到 MissionDecision 的映射。
  - [ ] 覆盖 wait-resume 恢复。
  - [ ] 覆盖接管记录进入 audit / replay。
