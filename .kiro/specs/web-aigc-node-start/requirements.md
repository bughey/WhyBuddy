# 需求文档：开始节点

## 目标

定义 `start` 节点迁移到 Cube 后的最小运行时需求。

## 需求

### 需求 1：入口职责

系统应允许 `start` 节点作为编排实例的唯一入口，负责接收全局输入并初始化上下文。

### 需求 2：输入输出

`start` 节点应支持接收用户输入、附件、全局变量，并输出标准化运行上下文。

### 需求 3：可观测性

`start` 节点应写入实例启动事件与初始输入快照。
