# 任务清单：Mission 模型到任务自动驾驶模型映射

- [ ] 梳理当前主仓中 `Mission` 的核心字段、状态和上下文来源，形成到 `Destination` 的字段映射表
- [ ] 梳理当前主仓中 `Workflow` 的定义结构、阶段语义、分支与并行能力，形成到 `Route` 的映射表
- [ ] 梳理当前运行时中的实例状态、节点状态、等待输入状态、复核状态，形成到 `Drive State` 的高层状态归并规则
- [ ] 梳理当前 `Decision / HITL / approval / resume` 链路，形成统一的 `Takeover` 对象定义
- [ ] 定义 `Destination` 的最小展示结构，包括目标、约束、成功标准、缺失信息、预期交付物
- [ ] 定义 `Route` 的最小展示结构，包括主路线、可替代路线、阶段、风险点、接管点
- [ ] 定义 `Drive State` 的最小状态集合与状态切换说明
- [ ] 定义 `Takeover` 的最小类型集合，包括澄清、确认、审批、权限、预算、验收、异常接管
- [ ] 明确哪些映射可以在服务端 projection 层完成，哪些更适合在前端 view model 层完成
- [ ] 输出一版“兼容优先”的迁移原则说明，明确不建议立即大规模底层改名
- [ ] 为 README、架构图、驾驶舱 IA 后续更新准备统一术语表
- [ ] 补充一版面向 Web-AIGC 节点体系的附录，说明节点如何被重新包装为路线阶段或车队角色
- [ ] 识别现有 API、测试、运行时实现中对旧命名的强依赖，形成后续改造风险清单
- [ ] 评估 `Destination / Route / Drive State / Takeover` 四类投影对象的最小落地顺序
- [ ] 为后续 spec 提供依赖说明，明确与驾驶舱、路线推荐、接管面板、运行时投影等 spec 的边界关系
