# 设计文档：获取位置信息节点

## 设计概述

`get_location_info` 是强依赖宿主环境和授权的节点，在 Cube 当前主场景中不是优先项。

## 接口映射

- `web-aigc` 节点：`get_location_info`
- Cube 承接：前端位置能力适配器

## 运行流程

1. 请求位置授权
2. 获取位置数据
3. 过滤或模糊化敏感信息
4. 输出位置结果
