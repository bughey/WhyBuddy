# 任务清单：任务自动驾驶成功度量体系

## 指标定义与共享模型

- [ ] 定义成功度量体系的术语表与统一中文口径
- [ ] 定义 `AutopilotMetricEvent` 归一化事件模型
- [ ] 定义 `AutopilotMissionMetrics` 任务级样本模型
- [ ] 定义 `AutopilotMetricsAggregate` 聚合结果模型
- [ ] 定义样本纳入规则与排除原因枚举
- [ ] 定义 `complete / partial / missing / conflicted` 数据完整性状态

## 核心指标口径

- [ ] 定义任务送达率的分子、分母、时间锚点与终态枚举
- [ ] 定义接管率的必需接管与建议接管边界
- [ ] 定义用户确认次数的动作范围与排除规则
- [ ] 定义重规划率的判定标准与 `retry / replan` 区分规则
- [ ] 定义偏航率的判定标准与“计划内分支”排除规则
- [ ] 定义完成时长的主口径与阶段拆分口径
- [ ] 定义结果复核通过率的最终通过与首轮通过规则

## 事实源映射

- [ ] 定义 `mission -> metric event` 的映射规则
- [ ] 定义 `runtime -> metric event` 的映射规则
- [ ] 定义 `audit -> confirmation evidence` 的映射规则
- [ ] 定义 `replay -> drill-down evidence` 的映射规则
- [ ] 定义多源冲突优先级与降级策略
- [ ] 定义主事实缺失时的 `partial` 样本处理规则

## 事件归一化与派生计算

- [ ] 设计归一化事件生成器的输入输出契约
- [ ] 设计任务级样本构建流程
- [ ] 设计接管次数、确认次数、重规划次数、偏航次数的派生逻辑
- [ ] 设计阶段耗时拆分逻辑
- [ ] 设计复核最终结论与首轮结论的派生逻辑
- [ ] 设计偏航事件与重规划、接管后续动作的关联规则

## 聚合与查询

- [ ] 设计时间窗口聚合能力
- [ ] 设计按自动驾驶等级的切片聚合
- [ ] 设计按任务类型、路线家族、工作区、环境的切片聚合
- [ ] 设计任务样本列表查询能力
- [ ] 设计聚合指标钻取到任务样本的查询能力
- [ ] 设计聚合结果中的 `avg / p50 / p90` 时长统计能力

## 回放与审计集成

- [ ] 设计任务样本到 replay 深链的关联字段
- [ ] 设计任务样本到 audit 深链的关联字段
- [ ] 设计 dashboard 指标与 replay 时间线的一致性校验规则
- [ ] 设计 dashboard 指标与 audit 确认事件的一致性校验规则
- [ ] 设计关键治理事件的证据引用展示结构

## 数据质量与回补

- [ ] 设计测试样本、演示样本、回灌样本的排除策略
- [ ] 设计历史样本回补证据的流程
- [ ] 设计 `partial` 样本升级为 `complete` 的规则
- [ ] 设计 `conflicted` 样本的标记、告警与人工核查流程
- [ ] 设计指标口径变更时的版本化策略

## 接口与消费层

- [ ] 设计成功度量聚合接口的返回结构
- [ ] 设计任务级成功样本详情接口的返回结构
- [ ] 设计 cockpit / telemetry dashboard 的消费契约
- [ ] 设计 replay 侧栏消费成功度量样本的契约
- [ ] 设计 audit 侧消费确认次数与治理证据的契约

## 验证与测试计划

- [ ] 补充任务送达率口径验证用例
- [ ] 补充接管率与用户确认次数口径验证用例
- [ ] 补充重规划率与偏航率口径验证用例
- [ ] 补充完成时长阶段拆分验证用例
- [ ] 补充结果复核通过率验证用例
- [ ] 补充多源冲突与降级处理验证用例
- [ ] 补充 replay / audit / dashboard 三方对账验证用例
- [ ] 补充历史数据回补后的口径稳定性验证用例

## 文档与治理

- [ ] 将成功度量体系作为后续 autopilot cockpit / replay / audit specs 的依赖前置
- [ ] 在相关 spec 中引用统一指标口径，避免重复定义
- [ ] 补充面向产品、运营、治理的指标解释文案
- [ ] 补充口径变更记录与版本说明模板
