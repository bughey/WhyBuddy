# 需求文档：结束节点

## 目标

定义 `end` 节点在 Cube 中的结束收敛行为。

## 需求

### 需求 1：结束职责

系统应允许 `end` 节点作为编排图的合法结束点，收敛最终输出。

### 需求 2：输出汇总

`end` 节点应支持汇总最终变量、产物引用、关键摘要与状态。

### 需求 3：回放与报告

`end` 节点应写入实例完成事件，并为报告与回放提供收敛结果。
