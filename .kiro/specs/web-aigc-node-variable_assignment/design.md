# 设计文档：变量赋值节点

## 设计概述

`variable_assignment` 节点是图运行时中的数据组织节点，应服务于后续条件、提示词与外部调用节点。

## 接口映射

- `web-aigc` 节点：`variable_assignment`
- Cube 承接：图运行时上下文存储

## 运行流程

1. 读取源值
2. 执行转换表达式
3. 写入目标变量
4. 输出赋值结果摘要
